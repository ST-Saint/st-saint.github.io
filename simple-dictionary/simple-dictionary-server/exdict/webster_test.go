package exdict

import (
	// "bytes"
	// "fmt"
	// "os"
	"testing"
)

func TestRequest(t *testing.T) {
	RequestAudio("contract")
	// buf := bytes.NewBuffer(nil)
	// buf.WriteString("test")
	// var b []byte = buf.Bytes()
	// f, err := os.Create("test.bak") //创建文件
	// if err != nil {
	// 	fmt.Println(err)
	// }
	// f.Write(b)
}
